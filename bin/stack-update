#! /usr/bin/env sh

brew update \
  && brew bundle install \
  && brew upgrade \
  && brew cleanup --prune=all \
  && brew bundle cleanup --force \
  && brew doctor

command -v mas > /dev/null && mas upgrade

mise install

rustup update
