#! /usr/bin/env sh

brew update \
  && brew bundle install \
  && brew upgrade \
  && brew cleanup \
  && brew bundle cleanup --force \
  && brew doctor

asdf update
asdf plugin-update --all

# mas upgrade
