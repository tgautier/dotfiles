#! /usr/bin/env sh

brew update \
  && brew bundle install \
  && brew upgrade \
  && brew cleanup --prune=all \
  && brew bundle cleanup --force \
  && brew doctor

mas upgrade

mise install

rustup update
